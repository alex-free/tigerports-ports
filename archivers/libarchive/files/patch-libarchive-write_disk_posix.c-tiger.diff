--- libarchive/archive_write_disk_posix.c.orig	2026-08-05 20:15:44.000000000 -0600
+++ libarchive/archive_write_disk_posix.c	2026-08-05 20:47:28.000000000 -0600
@@ -123,10 +123,17 @@
 
 #if __APPLE__
 #include <TargetConditionals.h>
+#include <AvailabilityMacros.h>
 #if TARGET_OS_MAC && !TARGET_OS_EMBEDDED && HAVE_QUARANTINE_H
 #include <quarantine.h>
 #define HAVE_QUARANTINE 1
 #endif
+/* Mac OS X 10.4 does not have O_SYMLINK, but if these are defined it is expected to have it. */
+#if MAC_OS_X_VERSION_MIN_REQUIRED == MAC_OS_X_VERSION_10_4
+#undef HAVE_COPYFILE_H
+#undef HAVE_SYS_XATTR_H
+#undef HAVE_SYS_ACL_H
+#endif
 #endif
 
 #ifdef HAVE_ZLIB_H
